html
  body
    input(id='ask')
    input(type='button',onclick='ask()')
    script(src="/socket.io/socket.io.js")
    script.
      var socket = io();
      socket.on('display', function(data){
        eval(data);
      });
      function ask(){
        var askString = document.getElementById('ask').value;
        if(askString!=null && askString != ''){
          alert(askString);
          var data  ={}；
          data.topic = 'ask';
          data.text = askString;
          socket.emit("message",data);
        }
      }
